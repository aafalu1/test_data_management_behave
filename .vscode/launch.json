{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Behave",
      "type": "python",
      "request": "launch",
      "module": "behave",
      "console": "integratedTerminal",
      "args": [
        "${workspaceFolder}/features/",
        "--tags=@validation"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}